interface:
  display_name: "Visual QA"
  short_description: "Run browser visual QA with evidence capture"
  default_prompt: "Use $visual-qa to walk through this web app, capture visual evidence, and report UX or layout issues."
policy:
  allow_implicit_invocation: true
