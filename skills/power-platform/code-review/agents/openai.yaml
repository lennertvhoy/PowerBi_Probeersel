interface:
  display_name: "Power Platform Code Review"
  short_description: "Audit code for wiring gaps and unfinished work"
  default_prompt: "Use $code-review to audit this codebase for dead wiring, placeholders, silent failures, and missing verification."
policy:
  allow_implicit_invocation: true
